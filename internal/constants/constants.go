package internal

// Fps defines frames per second
const Fps = 60

// InitialSpeed defines the initial speed of snake in fps
const InitialSpeed = 5

// MaxSpeed defines the max speed of snake in fps
const MaxSpeed = 40

// InitialLength defines the initial length of snake
const InitialLength = 3

// MaxLength defines the maximum length of snake
const MaxLength = 40

// MinWidth defines the minimum required width of the window to run the game
const MinWidth = 100

// MinHeight defines the minimum required height of the window to run the game
const MinHeight = 35
